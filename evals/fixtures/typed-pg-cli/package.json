{
  "name": "pgtypes",
  "version": "0.4.1",
  "description": "Generate TypeScript types from a live Postgres schema.",
  "bin": { "pgtypes": "./src/cli.js" },
  "license": "MIT",
  "dependencies": { "pg": "^8.11.0" },
  "scripts": { "test": "node --test" }
}
