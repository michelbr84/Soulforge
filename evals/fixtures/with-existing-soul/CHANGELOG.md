# Changelog

## 0.4.0
- Added `--config` support. Held this off for three releases; enough people asked.
- Better error when the connection string lacks `information_schema` access.

## 0.3.0
- Emit `| null` for nullable columns instead of making everything optional.

## 0.2.0
- Enums.

## 0.1.0
- First release. 200 lines and one command.
